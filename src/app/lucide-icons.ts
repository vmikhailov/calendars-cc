// src/app/lucide-icons.ts
import {
  User, Users,
  CheckCircle, XCircle, AlertTriangle, AlertCircle, Info, Check, X,
  Plus, Trash2, Play, Pause, RefreshCw, Save, Copy, Upload, Edit, Download, Filter, Loader,
  ArrowRight, Calendar, CalendarDays, Clock, MapPin, Settings, FileText, Code, CreditCard, Package,
  Sun, Moon, Monitor, Eye, EyeOff, LogOut, ChevronUp, Menu
} from 'lucide-angular';

// User & People Icons
export const userIcons = { User, Users };

// Status & Feedback Icons
export const statusIcons = { CheckCircle, XCircle, AlertTriangle, AlertCircle, Info, Check, X };

// Action Icons
export const actionIcons = { Plus, Trash2, Play, Pause, RefreshCw, Save, Copy, Upload, Edit, Download, Filter, Loader };

// Navigation & Interface Icons
export const navigationIcons = {
  ArrowRight, Calendar, CalendarDays, Clock, MapPin, Settings, FileText, Code, CreditCard, Package, Menu
};

// System & Theme Icons
export const systemIcons = { Sun, Moon, Monitor, Eye, EyeOff, LogOut, ChevronUp };